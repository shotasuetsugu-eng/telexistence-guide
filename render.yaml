services:
  - type: web
    name: telexistence-installation-guide
    env: node
    plan: free
    buildCommand: corepack enable && corepack pnpm install --frozen-lockfile && corepack pnpm build
    startCommand: node dist/index.js
    envVars:
      - key: NODE_ENV
        value: production
